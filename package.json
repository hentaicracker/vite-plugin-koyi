{
    "name": "koyi",
    "private": true,
    "version": "0.0.1",
    "description": "AI-powered frontend development assistant with visual DOM context",
    "scripts": {
        "build": "pnpm --filter vite-plugin-koyi build",
        "build:client": "pnpm --filter vite-plugin-koyi build:client",
        "dev": "pnpm build:client && pnpm --filter playground dev",
        "dev:plugin": "pnpm --filter vite-plugin-koyi dev:client",
        "playground": "pnpm --filter playground dev",
        "typecheck": "pnpm --filter vite-plugin-koyi typecheck",
        "release": "pnpm --filter vite-plugin-koyi build && cd packages/vite-plugin-koyi && npm version patch && npm publish --access public --registry https://registry.npmjs.org/"
    },
    "devDependencies": {
        "typescript": "^5.4.5"
    }
}